# Default configuration for single phase testing
phase_numbers: [1]  # Can be overridden
models: ["gpt4", "claude", "gemini"]
validation_mode: "balanced"
process_by_room: true
timeout_seconds: 300
retry_attempts: 2
save_outputs: true
output_directory: "test_outputs"
test_name: "default_single"
description: "Default configuration for testing a single phase with all models"