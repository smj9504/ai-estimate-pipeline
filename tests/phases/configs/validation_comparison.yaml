# Configuration for comparing validation modes
phase_numbers: [1]
models: ["gpt4", "claude"]
validation_mode: "balanced"  # Will be overridden in comparison tests
timeout_seconds: 300
retry_attempts: 2
save_outputs: true
output_directory: "test_outputs"
test_name: "validation_comparison"
description: "Configuration for comparing strict, balanced, and lenient validation modes"